# Lean CI/test requirements: production deps + pytest only.
# (numpy/sklearn/lxml/playwright are NOT imported by the test suite, so they are
# intentionally excluded to keep CI fast.)
-r requirements.txt

pytest>=8.0.0
# Fails any test that hangs (e.g. an accidental live network call) instead of
# stalling all of CI.
pytest-timeout>=2.3.0
